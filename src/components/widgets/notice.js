import React from "react"
// import { graphql } from "gatsby"

const NoticeComponent = ({}) => {
  return (
    <div className="notice flex column-mobile">
      <div className="icon">sdsdsdsd</div>
      {/* <p>${item?.elements?.notice_text?.value}</p> */}
      <p>dssdsdadas</p>
    </div>
  )
}

export default NoticeComponent
